import React from 'react';

const HomePage = () => {
    return (
        <div>
            <h1>home</h1>
            <button className='btn btn-danger'>Logout</button>
        </div>
    );
};

export default HomePage;